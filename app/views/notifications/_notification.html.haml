.notification-view
  = notification_form(notification)
  %span.moderate-font= " (#{time_ago_in_words(notification.created_at)}前)"
  %br/
  - if !@comment.nil?
    %p.notification-comment= @comment

